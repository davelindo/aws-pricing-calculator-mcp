{
  "name": "aws-pricing-calculator-mcp",
  "version": "4.0.0",
  "private": true,
  "description": "MCP server for universal AWS architecture interpretation, compositional pricing, official calculator links, and estimate validation.",
  "type": "module",
  "main": "src/index.js",
  "bin": {
    "aws-pricing-calculator-mcp": "./bin/aws-pricing-calculator-mcp.js"
  },
  "scripts": {
    "start": "node ./bin/aws-pricing-calculator-mcp.js",
    "chatbot": "node ./src/chatbot/server.js",
    "lint": "node ./scripts/lint.mjs",
    "contracts:generate": "node ./scripts/generate-contracts.mjs",
    "contracts:check": "npm run contracts:generate && node --test test/contract-v2.test.js",
    "coverage:live": "node scripts/audit-live-calculator-coverage.mjs",
    "test": "node --test",
    "test:live": "AWS_CALCULATOR_LIVE=1 node --test test/live-roundtrip.test.js",
    "check": "npm run lint && npm run contracts:check && npm test",
    "worker:deploy": "./scripts/deploy-worker.sh",
    "worker:dev": "npx wrangler dev --config wrangler.toml"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0",
    "jose": "^6.2.2",
    "zod": "^4.1.5"
  }
}
